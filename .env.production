# Used automatically by `vite build` (production mode). The dev-only
# proxy in vite.config.js does not exist in the built site, so this
# must be the real, absolute API origin.
VITE_API_BASE_URL=https://api.hlohealth.in/api
